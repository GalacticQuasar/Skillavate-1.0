<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
		<meta name="theme-color" content="#008ca8" />
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" />
		<link rel="stylesheet" href="/style.css" />
		<link rel="stylesheet" href="/style.css" />
		<link rel="icon" href="/skillavate-icon.ico" />
		<title>Skillavate</title>
	</head>
	<body>
		<!-- Header -->
		<%- include('partials/header') %>

		<div class="container" style="width: 1000px; margin-top: 150px">
			<div class="row">
				<div class="col-7 skillavate-div" style="padding: 25px">
					<h1 style="color: var(--primary-color)" style="text-align: center"><%= serviceTitle %></h1>
					<hr style="border: 2px solid var(--light-color)" />
					<p style="color: var(--light-color)"><%= serviceDescription %></p>
				</div>

				<div class="col-1" style="min-height: 100%; height: 100%">
					<!-- <div class="d-flex" style="height: 200px">
						<div class="vr"></div>
					</div> -->
				</div>

				<div class="col skillavate-card align-self-center">
					<h2 style="color: var(--primary-color)">Contact Information:</h2>
					<hr style="border: 2px solid var(--dark-color)" />
					<p style="font-size: large">Name: <%= serviceName %></p>
					<p style="font-size: large"><%= servicePhone != "" ? "Phone Number: " + servicePhone : ""%></p>
					<p style="font-size: large"><%= serviceWebsite != "" ? "Website: " + serviceWebsite : ""%></p>
					<p style="font-size: large"><%= serviceEmail != "" ? "Email: " + serviceEmail : ""%></p>
				</div>
			</div>
		</div>

		<!-- <div class="container mt-4 skillavate-card-expanded">
			<h1 style="color: var(--primary-color)"><%= serviceTitle %></h1>
			<p><%= serviceDescription %></p>
			<h2>Location</h2>
			<p><%=serviceLocation %></p>

			<hr style="border-top: 3px solid var(--dark-color)" />

			<h2>Contact Information:</h2>
			<p style="font-size: large"><%= servicePhone != "" ? "Phone Number: " + servicePhone : ""%></p>
			<p style="font-size: large"><%= serviceWebsite != "" ? "Website: " + serviceWebsite : ""%></p>
			<p style="font-size: large"><%= serviceEmail != "" ? "Email: " + serviceEmail : ""%></p>
		</div> -->

		<!-- Footer -->
		<%- include('partials/footer') %>
	</body>
	<script></script>
</html>
